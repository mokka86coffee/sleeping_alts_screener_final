# Файлы проекта

Составлено 25 августа 2026 по снимкам каталога. Известное описано по
докстрокам и по работе с этими файлами; про остальное сказано прямо,
что не знаю, — догадка в описи хуже пробела, потому что выглядит как
знание.

---

# ЧТО ЗАПУСКАТЬ РУКАМИ

## По расписанию — напоминает сам прогон

`run.py` печатает список в начале работы и только то, чему срок
подошёл. Отметить сделанным: `python run.py --done КЛЮЧ`.
Сроки и причины — в `analytics_manual.py`.

| Срок | Что | Ключ |
|---|---|---|
| ежедневно | сверить делистинги и наблюдательные метки бирж | `listing` |
| раз в 3 дня | записать решения и причины в `decisions.json` | `journal` |
| раз в неделю | дописать долю стейблов в `reservoir.json` | `reservoir` |
| раз в неделю | сверить разлоки в `unlocks.json` | `unlocks` |
| раз в неделю | обновить частокол в `events.json` | `events` |
| раз в 2 недели | закрыть чужие прогнозы в `predictions.json` | `predictions` |

## Отдельные скрипты, не входящие в прогон

**`fundamental_revenue.py`** — выручка протокола против оборота по
монетам журнала. Пишет `fundamental_revenue.json`. Запускать редко:
база протоколов меняется медленно, а сводка длинная.

 — замеры по журналу, пишет
`measure_journal.csv`. Судя по назначению, для проверок задним
числом; ТОЧНОЕ УСТРОЙСТВО НЕ ЗНАЮ.

**`flow_replay_btc.py`** — прогон детекторов по истории, результат в
`flow_replay_btcusdt.csv`. Инструмент проверки, не прогона.

**`portfolio_start.py`** — условный портфель на текущем составе
журнала. Проставляет правило «на выходных не заходим» задним числом и
**честно оставляет пустым то, что не восстановимо**: каким был первый
разгон девять дней назад, из записи не следует, и догадку в метрику,
которой потом верить, скрипт не подставляет. Печатает два портфеля
рядом — механический и по правилам, — иначе непонятно, что дало
правило. Меняет ровно одно поле, проверяет это ДО записи и делает
резервную копию.

**`add_missing_leaders.py`** — по имени заполнитель журнала, устройства
не знаю.

## Файлы, которые правятся руками

`unlocks.json` · `reservoir.json` · `events.json` · `decisions.json` ·
`demand.json` · `hl_whales.json` (список адресов китов Hyperliquid с подписями: плечо, цена входа, уровень ликвидации) ·
`bubble_manual.json` · `predictions.json`

---

## Точка входа и рассылка

**`run.py`** — оркестрация прогона: отбор символов, параллельный
анализ, воронка, снимок, отчёт, публикация. Ключи `--loop`,
`--limit`, `--symbols`, `--no-html`, `--no-git`, `--done`.

**`send_brief_telegram.py`**, **`send_brief_email.py`** — отправка
брифа.

---

## Ядро

**`core_binance.py`** — тикеры и свечи с биржи.
**`core_config.py`** — настройки, пороги, пути.
**`core_http.py`** — запросы и `log`.
**`core_models.py`** — `Candidate`, `FunnelStage`, `RunSnapshot`.

---

## Аналитика

### Данные и признаки
- **`analytics_metrics.py`** — снимок по монете: сырые числа плюс
 форматированные. Опора для четырёх модулей.
- **`analytics_indicators.py`** — индикаторы, чистые функции без сети.
- **`analytics_pulse.py`** — показания: неделя плюс архив 30 дней.
 Самый востребованный, к нему обращаются пять модулей.
- **`analytics_levels.py`** — потолок, опора, границы диапазона.
  Цели ставятся туда, где цена УЖЕ разворачивалась, а не на
  произвольном множителе риска. Но уровни ещё и фильтруются по
  отношению риска: цель ближе стопа торгово бессмысленна, сколько бы
  раз её ни тестировали.
- **`analytics_liqmap.py`** — кластеры плеча, смоделированные из
  обычных свечей. Отличать от `analytics_coinglass`: тот читает
  ФАКТИЧЕСКИЕ ликвидации от бирж, этот — ОЦЕНКУ уровней, где плечо
  стоит сейчас.
  Внутри записана поправка, без которой карта читается неверно:
  скопление плеча НЕ означает, что цена туда пойдёт и пробьёт. Там
  может стоять крупный участник, набирающий позицию. Плита — место,
  где ЧТО-ТО произойдёт: снимут или защитят. Какое из двух, отвечает
  не карта, а реакция на уровень.
- **`analytics_intraday.py`** — что происходит прямо сейчас.
- **`analytics_momentum.py`** — момента-величины, общие для карточки,
 зала, орбиты и отчёта.

### Детекторы и правила
- **`analytics_squeeze.py`** — заряд, разряд, перегрев, поглощение;
 усилие против результата, тест Вайкоффа.
- **`analytics_flow.py`** — отбор и порядок семейства FLOW.
- **`analytics_permission.py`** — разрешение рынка и индекс альтсезона.
- **`analytics_reservoir.py`** — гейт стейблкоинов: вышел ли капитал
 из обороны.
- **`analytics_calendar.py`** — частокол событий.
- **`analytics_unlocks.py`** — разлоки: даты, доли, получатели.
  **Единственная величина в проекте, которая смотрит ВПЕРЁД**: всё
  остальное описывает прошлое и надеется, что оно повторится.
  Расписание известно заранее и от наших расчётов не зависит.
- **`analytics_listing.py`** — состояние листинга: делистинги и метки
 бирж. Важнее разлока.
- **`analytics_demand.py`** — структурный спрос, обратная сторона
 разлоков.
- **`analytics_link.py`** — связка разлока и открытого интереса.
  Опасна СВЯЗКА, а не части: растущий интерес сам по себе накопление,
  транш на обороте переваривается за часы, вместе — конструкция для
  выноса. Читает не уровень плеча, а его УДЕРЖАНИЕ рядом с датой
  транша. Скор не трогает намеренно: наружу идёт отметка с числами,
  решение за человеком.
- **`analytics_scoring.py`** — скоринг, каждое слагаемое именовано и
  сохраняется отдельно, чтобы карточка показала разбор, а не только
  итог. Внутренние шкалы детекторов приводятся к общей: FLOW из
  сорока пяти в четырнадцать-тридцать четыре, TAIKO в пятнадцать-
  тридцать пять, DEXE из пятидесяти пяти в тот же диапазон.
- **`analytics_veto.py`** — **отделено от скоринга сознательно**. Скор
  отвечает «насколько интересно», вето — «можно ли вообще». Монета с
  высоким скором и активным вето остаётся в отчёте ВИДИМОЙ, но
  помеченной.
  Пять причин: сквиз, перегретый фандинг, тонкая ликвидность,
  запредельная волатильность и отсутствие спота. У каждой своя
  тяжесть, а блокирует только та, что попала в
  `VETO_BLOCKING_SEVERITY`.
- **`analytics_rr.py`** — соотношение риска к прибыли. Только
  математика: шкала заполнения логарифмическая, потому что разница
  между один к одному и один к двум ощущается сильнее, чем между один
  к восьми и один к девяти.

### Решения и деньги
- **`analytics_action.py`** — что делать с монетой сегодня.
- **`analytics_size.py`** — размер позиции как правило. Три ступени,
  названные причины, ноль моделей риска — грубость намеренная: задача
  не в точности, а в том, чтобы разница между монетами перестала быть
  невысказанной.
  Ступень понижается на одну за КАЖДЫЙ УРОВЕНЬ (рыночный, монетный), а
  не за каждый признак: иначе три признака сразу выбросили бы позицию
  в ноль, а это уже вердикт «не входить», которого правило не выносит.
  Ниже четверти не опускается.
  Там же план входа частями: чем хуже окно, тем меньше первая часть —
  не от страха, а потому что при тонком стакане цена вероятнее будет
  ниже, и вторая часть достанется дешевле.
- **`analytics_exit.py`** — правило выхода: «пора смотреть».
- **`analytics_strategy.py`** — вход, стоп, цели. Цели строятся ОТ
  СТРУКТУРЫ рынка, а не от множителей риска. Если структура не даёт
  цели с приемлемым соотношением, план помечается непригодным: лучше
  пропустить монету, чем нарисовать сделку с отрицательным ожиданием.
- **`analytics_portfolio.py`** — HOLD и трейдинг: не «мои против
  машинных», монеты одни и те же. Разные ПОДХОДЫ. HOLD — попал в
  лидеры, взял, держу, выходов нет вовсе. Трейдинг — те же монеты по
  правилам, с пустого счёта и со своей книгой.
  Счета раздельные намеренно: правила иногда закрывают и открывают
  заново, у трейдинга появляются собственные точки входа. На одной
  ленте это дало бы число, не описывающее ни одну из двух историй.
- **`analytics_candidate.py`** — сборка кандидата.

### Память системы
- **`analytics_decisions.py`** — журнал решений.
- **`analytics_actionlog.py`** — журнал предположений стратегии.
- **`analytics_leaders.py`** — лидер прогона и аномальные объёмы.
- **`analytics_manual_fields.py`** — поля, которые код узнать не
  может, с разбором ТРЁХ причин: данных нет у биржи (история
  открытого интереса только за тридцать дней), данные за окном
  расчёта (карта зон видит двести сорок дней, полки старше работают
  дальше), и это суждение, а не величина (был цикл или листинговый
  распил). Модуль ничего не считает — только описывает, каких полей
  не хватает, и считает пробелы по журналу.
- **`analytics_manual.py`** — напоминания о ручной работе со сроками.

### Внешние срезы
- **`analytics_coinglass.py`** — перекос ликвидаций.
- **`analytics_hyperliquid.py`** — перекос китов.
- **`analytics_flatwatch.py`** — мост к пузырь-боту.

### Сборка экранов
- **`analytics_stars.py`** — данные для зала и карточек, один расчёт
 на два экрана. Тянет 17 модулей.

---

## Семейство FLOW — сердце отбора

Устроено как диспетчер с подкейсами. Контекст считается **один раз**,
подкейсы прогоняются по нему, сильнейший становится вердиктом. Ни один
подкейс не считает сам сверх своей фигуры, не ходит в сеть и не знает
о соседях.

**Правило разграничения, записанное в контракте:** подкейс молчит,
если фигура не закрыта. Слабый сигнал хуже отсутствия — он разбавляет
топ и съедает место у монеты, где фигура собралась целиком.

### Основание
- **`detectors_flow.py`** — диспетчер, один сигнал на монету.
- **`detectors_flow_core.py`** — ядро: бары, события, зоны, поток,
 вортекс, контекст. Самый большой файл проекта, около тысячи девятисот
 строк.
- **`detectors_flow_config.py`** — все числа семейства. Единственное
 место, где живут пороги: в подкейсах магических констант нет.
- **`detectors_flow_signal.py`** — контракт подкейса: `SubcaseSignal`,
 протокол, общие вето. Здесь же признак `late` — фигура собралась, но
 описывает уже состоявшееся движение.

### Подкейсы — семь фигур
- **`_hidden`** — скрытое накопление: цена стоит, поток растёт.
- **`_churn`** — поглощение: объём приходит на уровень, цена не
 отвечает. Кто-то принимает поток лимитом.
- **`_spring`** — пружина: серия событий на узком диапазоне при
 затухающем фоне. Обратна churn по фону: там шум, здесь тишина.
- **`_fuel`** — топливо: уровни пройдены вверх и удержаны, сверху
 пусто. Предложение снято.
- **`_leverage`** — перекос в плече: фандинг отрицательный, интерес
 растёт, цена стоит. Шорты платят за право оставаться.
- **`_taker`** — смена стороны агрессора: доля рыночных покупок
 сдвинулась, цена не пошла. Отличие от hidden — доля, а не абсолют.
- **`_dormant`** — спячка: состояние ДО движения. Единственный
 подкейс, определяемый ОТСУТСТВИЕМ свидетельств.

---

## Отдельные детекторы, вне семейства

- **`detectors_taiko.py`** — разворот на старшем масштабе: падение от
 пика от шестидесяти процентов, зрелая база у дна либо разворотный
 импульс, подтверждение сигналом.
- **`detectors_dexe.py`** — фигура после пампа: рост от десяти дней и
 от десяти раз, затем дамп до тридцати часов глубиной от девяноста
 процентов, свежесть до четырёх дней.
- **`detectors_squeeze.py`** — выжимание шортов: связка цены,
 открытого интереса, фандинга и отношения спота к фьючерсам.
 ВНИМАНИЕ: это оценка РИСКА пампа, не сигнал ко входу.
- **`detectors_volume_surge.py`** — всплеск дневного объёма против
 среднего за тридцать дней, с нормировкой на прошедшую долю суток.

---

## Отрисовка

### Как собирается отчёт

Раньше была ОДНА страница: дашборд, а внутри него сводка, зал и
карточка, показываемые переключением видимости. Теперь — **набор
документов**: оболочка плюс по файлу на экран.

    index.html      оболочка: лоадер и пустой iframe
      └── dashboard.html   грузится первым
      └── brief.html       ↑ каждый в тот же iframe, по очереди
      └── podium.html      ↑ предыдущий при этом уничтожается

Сцена карточки осталась **внутри дашборда намеренно**: это не экран
очереди, а модальное окно, открываемое кликом. Свой документ ей мешал
бы — она обязана лежать поверх того, из чего её открыли.

Стили кладутся **в каждый документ целиком**, а не общим файлом:
изоляция стилей — половина смысла разделения, а общий подключаемый
файл вернул бы ту же связанность через сеть вместо дерева страницы.

**`render_page.py`** — сборка всех документов и **единственное место,
где данные считаются на все экраны сразу**. Звёзды считаются одним
вызовом и уходят и в сводку, и в зал, и в дашборд: второй источник тех
же чисел разошёлся бы при первой правке, и монета показывала бы на
двух экранах разное.

Порядок внутри строгий и объяснён: рынок первым, потому что в нём
живёт разрешение, которое звёздам нужно для ступени размера.

**Это и объясняет старый `output/index.html`.** Он остался от прежней
раскладки, когда страница была одна. Сейчас оболочка собирается
заново, и лишний файл в выводе — след, а не рабочая копия.

### Экраны
- **`render_shell.py`** — оболочка, очередь показа. Экраны друг о
  друге не знают, очередь ведёт она.
- **`render_dashboard.py`** — дашборд, внутри него сцена карточки.
- **`render_brief.py`** — сводка при входе. Данные приходят
  аргументом и вшиваются при сборке: у каждого iframe своё окно, и
  прежнее чтение глобальной переменной там не работает.
- **`render_podium.py`** — ворота, зал, поиск, переход в карточку.
- **`render_cardscene.py`** — сцена карточки.
- **`render_orbit.py`** — орбита, верхний экран дашборда.
- **`render_flow_report.py`** — отчёт стратегии FLOW карточками.
  Применяется только к FLOW: у других стратегий свои представления.

### Общее
- **`render_common.py`** — помощники и палитра стадий, общие для
  дашборда и орбиты. Заведён, чтобы разорвать круговой импорт:
  раньше орбита доставала их отложенным импортом из дашборда.
  **Палитра стадий здесь — единственный источник**; встроенные копии
  в зале и орбите запасные и обязаны совпадать.
- **`render_blocks.py`** — шапка, панель приборов, легенда, заголовки.
- **`render_funnel.py`** — воронка отбора и сектора. Радиус узла по
  корню доли: площадь читается честнее диаметра.
- **`render_theme.py`** — токены дизайна: **единственное место, где
  живут цвета, размеры и правила подачи чисел**. Модули отрисовки
  литеральных цветов не содержат. Там же типографика тикера — размер
  подбирается по длине имени, чтобы длинные не ломали шапку.
- **`render_css.py`**, **`render_svg.py`**, **`render_table.py`**,
  **`render_card.py`** — стили, примитивы, таблица, карточка.

---

## Источники

**`sources_storage.py`** — снимки прогонов, `latest.json` и сравнение с
предыдущим. Запись через временный файл: незавершённый прогон не портит
отчёт.

Там же **дневной архив, и причина его появления важна**. Ротация держит
только последние несколько прогонов — при прогоне каждые три часа это
считанные дни. А замеры «сигнал → ход» и «пропущенные против взятых»
строят распределения на НЕДЕЛЯХ. До двадцать второго августа материал
этих замеров **стирался самим хранилищем**. Теперь файл дня лежит вне
ротации и перезаписывается каждым прогоном — в архиве остаётся
последний снимок дня.

**`sources_coinglass.py`** — живые ликвидации по монетам журнала.
Ключ в `output/coinglass_config.json`, шаблон создаётся сам. Карта
уровней открыта только с дорогого тарифа: код готов и включится сам,
как только проба увидит доступ, а до того поле честно говорит «тариф
ниже». Проверка руками: `python sources_coinglass.py --probe`.

**`sources_hyperliquid.py`** — позиции китов. Устроен так, потому что
официальный интерфейс списка топов не отдаёт, но отдаёт позиции любого
адреса. Ручной список в `hl_whales.json` **неприкосновенен**, а секцию
`auto` прогон пополняет сам с доски.

Отбор идёт не по полям доски, а **проверкой позициями**: кандидат
берётся, только если у него есть живые перп-позиции. Причина записана:
верх доски по размеру счёта забит холдерами HYPE — перпов у них нет, и
перекос мерить не по чему.

Оговорка зашита в смысл поля: киты — КОНТЕКСТ, не сигнал к
копированию. Крупные счета бывают хеджированы, маркетмейкерят или
просто неправы.

**`sources_external.py`** — внешние данные, не читал.

**`sources_tracker.py`** — **НЕПОЛНЫЙ ФАЙЛ.** Сорок три строки:
докстрока и пять констант, **ни одной функции**. При этом описание
обещает журнал, который живёт между прогонами и накапливает историю.
Либо тело потерялось, либо модуль так и не дописан.

Второе расхождение там же: путь `BASE_DIR / "data" / "tracked.json"`,
а комментарий рядом говорит «рядом с отчётом: дашборд читает тот же
каталог». Отчёт лежит в `output/`, не в `data/`. Ровно та же ошибка
пути, что уже находили у календаря и резервуара, — стоит проверить,
существует ли каталог `data/` вообще.

---

## Инструменты

- **`bubble_bot.py`** — торговый полигон на бумажных деньгах.
- **`fundamental_revenue.py`** — выручка против оборота.

---

## Папка output — что кладёт прогон

**`runs/`** — снимки прогонов, по одному на запуск.
**`latest.json`** — последний снимок, из него живут экраны.
**`index.html`** — УСТАРЕЛ (проверено 25.08.2026). Папка вывода закрыта в `.gitignore`, публикуется копия из корня. Значит в выводе лежит страница прошлых прогонов, и открыв её по ошибке, увидишь старые данные без единого признака этого. Либо чистить, либо не писать сюда вовсе.

**Журналы**
- `actions_log.jsonl` — что стратегия сказала и когда.
- `leaders.json`, `leaders_archive.json` — лидер прогона и архив.
- `anomaly_volume.json` — аномальные объёмы.
- `unlocks_seen.json` — какие разлоки уже отмечены, чтобы не
 показывать один и тот же дважды.

**Пузырь-бот**
- `bubble_state.json` — состояние.
- `bubble_trades.jsonl` — сделки.
- `bubble_events.jsonl` — события.
- `bubble_report.txt` — отчёт.

**Мосты и срезы**
- `flat_watch.json` — список монет во флэте у дна, для бота.
- `flow_watch.json` — наблюдение семейства FLOW.
- `coinglass_state.json`, `coinglass_config.json` — срез ликвидаций.

**Доступы**
- `email_config.json`, `telegram_config.json` — настройки рассылки.

---

## Ручные инструменты — подробно

Ни один не входит в прогон. Все запускаются из корня проекта.

### Заполнение данных

 — постоянные поля `unlocks.json` по журналу
лидеров: доля обращения, отношение полной оценки к капитализации,
вершина и дно жизни с датами. Источник — открытый рыночный эндпоинт
CoinGecko, один запрос на всю выборку.

Главное свойство: **слияние, а не перезапись**. Скрипт трогает только
те ключи, которые считает сам, и НИКОГДА не касается `events` —
ручная работа не пропадает от запуска автомата. Список своих ключей
задан явно, чтобы добавление нового ручного поля не требовало правки.

Там же записано, почему разлоки остаются руками: секция разлоков у
DefiLlama закрыта подпиской, эндпоинты эмиссии в интерфейсе тоже —
триста долларов в месяц. Бесплатная страница показывает ближайшие дни
по всему рынку и наших монет обычно не содержит.

Оговорка про сопоставление честная: тикеры не уникальны, под символом
ACE живёт несколько токенов. Берётся запись с наибольшей
капитализацией, а её идентификатор остаётся в поле `source` — чтобы
ошибку сопоставления можно было увидеть глазами.

  python fill_unlocks.py --dry   показать, не записывая

**`fundamental_revenue.py`** — третий фильтр: откуда в монете деньги.
Сопоставляет монеты журнала с базой протоколов DefiLlama и печатает
отношение выручки за месяц к биржевому обороту.

Повод записан прямо в файле: у PROM был скор восемьдесят восемь,
оборот тридцать пять миллионов в сутки, восемьдесят семь процентов на
биржах — и выручка протокола шестьсот восемьдесят один доллар за
квартал. Деньги в монете есть, но они котировальные, а не
продуктовые.

Границы названы до запуска: «нет в базе» — это НЕ «выручки нет», а
«Llama не считает это протоколом». Мемкоин там не может быть по
определению.

### Проверка задним числом

**`flow_replay_btc.py`** — эталонный реплей по биткоину. Идёт по
дневкам срезами: на каждую дату детекторы видят только бары до неё.

Устроен **подменой источника, а не копией логики**: подставляет срезы
и зовёт неизменённый диспетчер. Причина записана — скопируй логику в
реплей, и через месяц проверялось бы не то, что торгуется.

И самое ценное — **известные ответы в шапке**, против которых читать
выдачу. Октябрь двадцать пятого, вершина сто двадцать шесть тысяч:
скринер обязан молчать, дальше минус сорок три процента. Июнь-июль
двадцать шестого, дно: накопление размечено независимо, здесь
молчание — ошибка. С прямым указанием, какая ошибка дороже.

 — замеры по каталогу снимков. Считает срок от
последнего сигнала до старта хода, сравнивает взятые монеты с
показанными-без-входа, и мера при этом **относительная**: из хода
монеты вычитается медианный ход выборки, иначе в дни прилива поедут
обе группы и разница окажется шумом.

Это ровно те замеры, о которых мы говорили весь день. Инструмент уже
есть.

### Отладка детекторов

 — прогон семейства по всем монетам, сводка в CSV и
разбор сработавших в JSON. Не фильтрует: задача — увидеть, где
подкейсы недобирают.

 — сырой срез по ОДНОЙ монете, символ задан руками.
Отличие от пробы принципиальное: там монета попадает, только если
прошла отбор; здесь условий нет вовсе — даже если все подкейсы
отказали, выгрузка состоится, и в ней будет написано, ПОЧЕМУ.

 — разбросы интрадей-слоя, по которым
выбираются пороги. Заведён потому, что интрадей живёт в метриках и в
дамп семейства не попадает вовсе.

### Прочее

 — ссылки на карты ликвидаций Coinglass по монетам
журнала. Карта читается только глазами: программный доступ открыт с
дорогого тарифа. Внутри записано правило чтения, выведенное на BLESS:
верхняя плита — шорты, нижняя — лонги, а пустая карта у спящей монеты
не «нет данных», а подтверждение фигуры.

 — интеграция CoinGecko и DefiLlama для
фундаментального разбора. Докстрока одна строка, устройство не читал.

---

# ЧЕГО НЕ ЗНАЮ — ПРИШЛИТЕ

Осталось шесть, и все второстепенные.

`analytics_buzz.py` — единственный из аналитики.

`render_card.py` · `render_svg.py` · `render_table.py` — примитивы
отрисовки; назначение видно по использованию у соседей, внутрь не
смотрел. `render_css.py` — стили целиком, читать незачем.

`external_data.py` · `sources_external.py` — по имени одно и то же
разными путями, возможно ещё один дубль.

`add_missing_leaders.py` · `apply_patch.py` ·
`replace_all_dot_imports.py` — разовые правки кода, а не части
системы.

# НА ЧТО СТОИТ ПОСМОТРЕТЬ ОТДЕЛЬНО

Не описание, а наблюдение по составу каталога.

**Дубли подтверждены построчной сверкой, и оба опасны.**

`dexe_detector.py` — СТАРАЯ версия `detectors_dexe.py`. Ходит в сеть
через requests в обход общего слоя, и в ней есть ошибка, которую в
новой уже починили: пик ищется по ПЕРВОМУ вхождению. Если цена
коснулась пика дважды, дамп искусственно удлиняется на весь промежуток
между касаниями. В новой версии стоит `_last_index_of` с объяснением
причины. Старую надо удалять, а не хранить рядом.

`detectors_debug_fundamentals.py` — это НЕ отладка фундаментала. Внутри
лежит подкейс спячки, с той же докстрокой, что у
`detectors_flow_dormant.py`, но на шестьдесят пять строк короче: в нём
нет порогов `DORMANT_LIFE_DROP_MIN`, `DORMANT_LIFE_GROWTH_MIN` и
`DORMANT_BASE_MIN`. То есть это устаревшая копия спячки под чужим
именем. Файл, чьё имя врёт о содержимом, хуже отсутствующего.

`sources_hyperliquid.py` рядом с `sources_hyperliquid-2.py` — не
сверял, но дефис в имени модуля делает его неимпортируемым обычным
способом, значит это либо копия, либо черновик.

**Код в папке вывода, а папка вне контроля версий.** `output/leaders.py`
и, судя по запуску `python3 output/fundamental_revenue.py`, скрипт
выручки лежат в `output/`. Папка закрыта в `.gitignore` — и это верно
для данных, но означает, что **эти два файла не под контролем версий
вообще**: ни истории, ни возможности откатить правку, ни копии в
репозитории.

Дальше хуже: папку вывода по смыслу можно очистить, и тогда код
пропадёт совсем — причём тихо. Отсутствие заметишь в тот момент, когда
понадобится запустить.

Место скрипта — в корне, рядом с остальными. В выводе только то, что
он произвёл: `fundamental_revenue.json` пусть остаётся, а
`fundamental_revenue.py` переехать.

**Доступы.** `email_config.json` и `telegram_config.json` лежат в
`output/`, которая закрыта в `.gitignore` — в публичный репозиторий не
уходят. Проверено 25.08.2026.

**Похоже на старые версии.** `render_cardscene_prev.py` и
`render_css_prev.py`. Если это резервные копии перед правкой, им
место в истории, а не в каталоге: рядом с рабочим файлом они рано или
поздно будут отредактированы по ошибке.

**Похоже на мусор.** `get-pip.py` — установщик пакетов, к проекту
отношения не имеет. `выборка с bless.csv` и `выборка с bless.json` —
имена с пробелами и кириллицей ломаются в командной строке и в путях.

**Чего в каталоге нет, а по коду должно быть.** `manual_state.json`
появится после первого `--done`. `reservoir.json` есть, но в нём одна
запись — из-за этого бриф третий день показывает одно число без
направления.
