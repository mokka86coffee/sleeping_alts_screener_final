# Патчи (26.09). A и B ВНЕСЕНЫ 12:10 по «да», C ВНЕСЕН 12:50 по «делай» — все ждут перезапуска прогона. Каждый — одна правка одного файла + строка в core_config.

## A. «Картина»: лонг только при медиане доски за 6 ч выше нуля (R27, leader_break_sight.py)
Откуда число: ноль — знак, не порог; в часы 6–24 после слома лидера лонги при доске6 ≤ 0 дают 43% / −0.83% (222 сделки), при доске6 > 0 — 62% / +0.55%;
в обычные дни с доской24 > +1% сделок при доске6 ≤ 0 всего 73 из 808 (71% в плюс). Суточная медиана после слома отстаёт и пропускает лонги.

core_config.py, рядом с SIGHT_BOARD_GATE:
```python
# ДОСКА ЗА 6 Ч (26.09, R27/leader_break_sight.py): после слома лидера суточная медиана ещё > +1%, а доска уже села — лонги «картины»
# в часы 6–24 после слома при медиане за 6 ч ≤ 0 дают 43% / −0.83% (222 сделки) против 62% / +0.55%. Ноль — знак, не выведенная мерка.
# Лонг только при медиане доски за 6 ч > SIGHT_BOARD6_GATE (вдобавок к суточной). None — выключено.
SIGHT_BOARD6_GATE = 0.0
```
paper_sight.py, импорт рядом с SIGHT_BOARD_GATE (строки 72–75):
```python
try:
    from core_config import SIGHT_BOARD_GATE, SIGHT_BOARD6_GATE
except ImportError:
    SIGHT_BOARD_GATE, SIGHT_BOARD6_GATE = None, None
```
paper_sight.py, в `_main` после `bg = background()` (строка 459):
```python
    # медиана доски за 6 ч — по архиву получасовок тех же монет (12 баров назад); суточная — из market_bg
    _ch6 = []
    for _s in syms:
        _r = rows_of(_s)
        if len(_r) >= 13 and _r[-1].get("px") and _r[-13].get("px"):
            _ch6.append((float(_r[-1]["px"]) / float(_r[-13]["px"]) - 1) * 100)
    bg["median6"] = (sorted(_ch6)[len(_ch6) // 2] if _ch6 else None)
```
paper_sight.py, блок «СТОРОНА ПО ФОНУ ДОСКИ» (строки 506–512) — добавить вторые ворота сразу после первых:
```python
        if s > 0 and SIGHT_BOARD6_GATE is not None and bg.get("median6") is not None and t > int(state["last_sig"].get(sym) or 0):
            if float(bg["median6"]) <= SIGHT_BOARD6_GATE:
                state["last_sig"][sym] = t
                _why = f"доска {float(bg['median6']):+.2f}% за 6 ч — лонг только выше {SIGHT_BOARD6_GATE:+.1f}% (R27)"
                events.append({"kind": "skip", "book": BOOK_LABEL, "sym": sym, "side": s, "t": t, "px": float(rows[-1]["px"]),
                               "at": now, "why_skip": _why, "score": sum(v.values()), "votes": v})
                s = 0
```
Проверка перед установкой: `.venv/bin/python paper_sight.py --only ARK` (без --write). Вступает в силу после перезапуска прогона владельцем.

## B. Журнал очереди: честная медиана доски за 24 ч (сейчас поле `board_med_24h` — ход с 00:00 UTC)
near_move.py, `_today_bars` (строка ~246): хранить все 80 строк, а не только сегодняшние, и посчитать ход за 24 ч:
```python
    rows, all_rows = [], []
    for line in p.read_text(encoding="utf-8").splitlines()[-80:]:
        try:
            r = json.loads(line)
        except ValueError:
            continue
        all_rows.append(r)
        if str(r.get("candle", ""))[:10] == today:
            rows.append(r)
    # ход за 24 ч — 48 получасовок назад (или самая ранняя из последних 49), для медианы доски в журнале
    _pxs = [r.get("px") for r in all_rows[-49:] if r.get("px")]
    px_chg_24h = (_pxs[-1] / _pxs[0] - 1) if len(_pxs) >= 25 else None
```
near_move.py, возвращаемый словарь (строка ~644), рядом с `"px_chg_pct"`:
```python
            "px_chg_24h_pct": round(px_chg_24h * 100, 1) if px_chg_24h is not None else None,
```
near_move.py, блок `_BG` (строки 1455–1462): суточная медиана — из нового поля, прежняя — под честным именем:
```python
    _chg_day = [float((vv.get("today") or {}).get("px_chg_pct")) for vv in (res.get("coins") or {}).values() if (vv.get("today") or {}).get("px_chg_pct") is not None]
    _chg = [float((vv.get("today") or {}).get("px_chg_24h_pct")) for vv in (res.get("coins") or {}).values() if (vv.get("today") or {}).get("px_chg_24h_pct") is not None]
    _chg.sort(); _chg_day.sort()
    _board_med = round(_chg[len(_chg) // 2], 2) if _chg else None
    _board_up = round(100 * sum(1 for x in _chg if x > 0) / len(_chg)) if _chg else None
    _board_day = round(_chg_day[len(_chg_day) // 2], 2) if _chg_day else None
    rows.append({..., "board_med_24h": _board_med, "board_up_pct": _board_up, "board_med_day": _board_day, "board_n": len(_chg), ...})
```
и в строке монеты (1487) `"board_med_24h": _board_med` остаётся, добавить `"board_med_day": _board_day`.
После установки: пересчитать state_probs.py / exclusion по честной доске (записи до патча — с 00:00 UTC, пометить).

## C. Полоса «скоро» по R34 — ВНЕСЕНО 26.09 12:50
core_config: `STAR_OI_JUMP_3H = 15.0` (с комментарием-источником). near_move `_today_bars`: `oi_jump_3h_pct` = интерес последнего бара к 6 барам назад (all_rows).
render_intro `collect_items`: после блока «В ТОПЕ ПОДРЯД» — для всех монет из near_move.json с `today.oi_jump_3h_pct ≥ STAR_OI_JUMP_3H` метка
«интерес +N% за 3 ч» + вероятности («спит: +10% за 48 ч у 52%, +40% у 15%» / «в ходу: 73% / 44%») и группа 0 (скоро); подпись группы «скоро» дополнена.
Не вход, а повод смотреть: у спящих 61% сигналов не дают и +10%.

## D–H. Книги бота, партия 1 — ВНЕСЕНО 26.09 13:40 по «правь пока только бота» (план: plan_screen_bot.md)
- D. core_config: PAPER_FAST_ENABLED / PAPER_BOTTOM_ENABLED = False (сделок нет с 23.09); run.py не запускает эти книги при флаге (пишет «на паузе»).
- E. «Толпа» (paper_crowd.py `rule_gate`): лонги выключены (46%); шорты — только «перекуплен» и только при медиане доски за сутки < −1% (73% там; market_bg).
- F. «Картина»: хедж по получасовке под флагом SIGHT_HEDGE_ENABLED = False (R24; ONE 13 хеджей). Открытые хеджи снимаются по-старому.
- G. «Картина»: третьи ворота — `leader_break_recent()` по всем монетам архива (мерки leader_break.py: лидер ≥+70%/48 ч, слом −15% от максимума суток, первый бар,
  доска остальных монет за сутки на баре слома > SIGHT_BOARD_GATE) → лонгов нет SIGHT_LEADER_BREAK_H = 24 ч (R27: 43% против 86%). Проверено: PHA 25.09 23:00 UTC, +2.3%.
- H. «3 в первых» `end_exit()`: R29 — при run_from_low7 ≥ 200% объём последней получасовки — максимум за сутки → выход; R21 — бар ≥ +15% и вынос шортов за час —
  максимум за сутки по потоку → выход. В --replay не работает (нет потока и near_move того времени).
Проверено без записи: paper_sight --only ARK,PHA,WAXP; paper_crowd --only 5 монет; paper_first3; прямые вызовы функций. Вступает после перезапуска.

## I–K. Книги бота, партия 2 — ВНЕСЕНО 26.09 14:05
- I. «Конец» разведён (paper_end.py `signal_split`, PAPER_END_SPLIT = True; старое правило «интерес −2% за бар» — 22% на 27 сделках — выключено):
  «конец-лидер» — монета = лидер, сломанный на ехавшей доске за последние 48 ч (paper_sight.leader_break_recent), шорт при более низком максимуме без
  объёма (объём бара ≤ медианы 48), интересе ниже, чем 6 баров назад, фандинге ≤ +0.02%, после отскока (цена выше, чем 3 ч назад) и закрытии вниз;
  цель — R4: 25% от максимума суток; размер 2.0. «сползание» — медиана фандинга за сутки ≤ −0.05%, цена ниже максимума 72 ч на 20%+, отскок от минимума
  6 баров ≥ +5% и бар закрылся ниже прошлого; цель — минимум 72 ч; срок 24 бара. Защита paper_guard «лидера не шортим» для ветки «конец-лидер» снята.
- J. Книга «интерес» (paper_interest.py, каркас paper_book_base.py): спящая монета (run_from_low7 < 20%), интерес +15% за 3 ч, средний тейкер трёх баров ≥ 1.2,
  цена за 3 бара не упала → лонг 500 $, цель +10%, стоп −10%, срок 96 баров, пауза 48 ч; mfe в журнале для счёта +40% (ожидание 52% / 15%).
- K. Книга «второй ход» (paper_second.py): после хода ≥ +40% пила ниже максимума 72 ч на 15%+, |медиана фандинга за сутки| ≤ 0.02%, интерес сдулся (мин 48 ч
  ниже макс 96 ч на 20%) и +5% за 12 баров, сумма дельты спота за 6 баров > 0 → лонг 500 $, цель — максимум 72 ч, стоп −12%, срок 192 бара, пауза 96 ч.
- run.py запускает обе после «3 в первых» (флаги PAPER_INTEREST_ENABLED / PAPER_SECOND_ENABLED). Все мерки — в core_config с источником (R-номер, случаи).
Проверено без записи: paper_end --only PHA,ONE,LSK,PLAY,ARK (слом PHA виден, сигналов сейчас нет); paper_interest, paper_second — прогон без сигналов, без ошибок.

## L. Пятый признак для четвёрок — ВНЕСЕНО 26.09 15:35 по «делай правки»
near_move.py `attach_today`: четвёрка без группы получает пятый признак «интерес рванул: +N% за 3 ч» при today.oi_jump_3h_pct ≥ STAR_OI_JUMP_3H (15%),
группа — по правилу judge (going / holding / pulled по положению цены к сбору). Источник: R34 (12% суток, 42/43 лидеров), four_signs.py (четвёрки — 8/71 лидеров
накануне). Проверено: `near_move.py --only RARE,WAXP` → RARE 5/5, going, в очереди. Вступает после перезапуска (вместе с D–K).

## M. Интро: метка «конец?» у пошедших — ВНЕСЕНО 26.09 15:55 по «вноси правки в интро»
render_intro `collect_items`: для звёзд группы 2 («пошли») вызывается `paper_first3.end_exit(sym)` (R29: рекордный объём получасовки при ходе ≥+200%;
R21: вынос шортов за час — максимум за сутки на вертикали ≥+15%); при срабатывании подпись и доводы начинаются с «КОНЕЦ? …», поле `end` в stars.json.
Те же проверки, что выход книги «3 в первых», — экран и бот говорят одно.

## N. Интро: строки состояния доски на панели фона — ВНЕСЕНО 26.09 16:05
render_intro `_board_state_notes()` → две строки в `bgnote` (рисуются как plainRow): «слом лидера» — последний настоящий слом на ехавшей доске за
SIGHT_LEADER_BREAK_H часов (та же функция, что у бота: paper_sight.leader_break_recent): «PHA 23:00 UTC при доске +2.3% · доска стоит или садится ~сутки
(4 из 4) · следующий лидер ~17 ч · лонгов «картины» нет ещё N ч (R27)»; «доска 6 ч» — медиана хода всех монет за 6 ч рядом с суточной и флаг «день доски»
по R28 (медиана за 6 ч на 06:00 UTC > +0.5% → 7 из 7). Проверено: render_intro() собирается, строки в DATA. Вместе с M («конец?») ждёт перезапуска.

## O. Выключатель правил paper_side — ВНЕСЕНО 26.09 16:15 (найдено при проверке после перезапуска)
SIDE_RULES_OFF глушил по префиксу «конец» новую ветку «конец-лидер» (PHA дал сигнал — пропущен «правило выключено») и «перекуплен» — единственный
оставшийся шорт «толпы». Из списка убраны «конец» и «перекуплен»; ветка переименована в «слом лидера». Осталось: «рост на выносе», «спайк на выносе», «против толпы».

