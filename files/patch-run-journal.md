# Патч · `run.py` — монеты журнала остаются в выборке

608 строк, поэтому патчем. Три блока, все внутри `select_symbols`.

**Почему не так, как в исходном патче.** Там предлагалось дописать
символы в `symbols` после отбора: `symbols.extend(extra)`, где `extra` —
список строк. Но `select_symbols` возвращает **пары** `(символ, оборот)`,
и `analyze_all` их распаковывает. На первой же журнальной монете
распаковка строки упала бы с `ValueError`. Плюс вставлять было некуда:
весь отбор целиком внутри `select_symbols`, снаружи готового списка не
существует.

Здесь добавка идёт парами и внутри функции, а оборот берётся из тех же
тикеров — журнальной монете он тоже нужен, её же передают в
`build_candidate`.

---

## 1. Импорт

### было

```python
from core.http import log
from core.models import Candidate, FunnelStage, RunSnapshot
```

### стало

```python
from analytics.leaders import tracked_symbols
from core.http import log
from core.models import Candidate, FunnelStage, RunSnapshot
```

---

## 2. Оборот запоминается до отсечки

Журнальная монета чаще всего выпадает именно по обороту, то есть до
`picked` не доходит. Значит её оборот надо запомнить раньше гейта —
иначе потом взять его будет неоткуда, а `build_candidate` его требует.

### было

```python
        if qvol < MIN_QUOTE_VOLUME_24H:
            stats["low_volume"] += 1
            continue

        picked.append((symbol, qvol))
```

### стало

```python
        # Оборот запоминается ДО отсечки. Монета журнала выпадает
        # обычно именно здесь — она затихла, — а оборот у неё всё
        # равно нужен: build_candidate принимает его вторым аргументом
        # и без него посчитает долю спота от нуля.
        vol_seen[symbol] = qvol

        if qvol < MIN_QUOTE_VOLUME_24H:
            stats["low_volume"] += 1
            continue

        picked.append((symbol, qvol))
```

---

## 3. Добавка журнала сверх лимита

### было

```python
    # Самые ликвидные вперёд
    picked.sort(key=lambda x: -x[1])
    picked = picked[:limit]
    stats["selected"] = len(picked)

    return picked, stats
```

### стало

```python
    # Самые ликвидные вперёд
    picked.sort(key=lambda x: -x[1])
    picked = picked[:limit]

    # ── Монеты журнала ──────────────────────────────────────
    # Добавляются сверх лимита и сверх порога по обороту.
    #
    # Иначе наблюдение обрывается на самом интересном месте: монета
    # попадает в журнал на всплеске, через несколько дней затихает,
    # выпадает из топа — и запись замирает с последними известными
    # числами. Карточка на орбите честно рисует прочерки, но чинить
    # надо не отображение, а состав выборки: журнал заведён ровно для
    # того, чтобы смотреть, чем кончилось.
    #
    # Стоимость ограничена сверху размером журнала: записи живут не
    # дольше LEADERS_MAX_AGE_DAYS, и пересечение с топом велико —
    # реальная добавка это единицы монет, а не сотня.
    #
    # Пара, а не голый символ: analyze_all распаковывает элементы
    # списка как (символ, оборот).
    have = {s for s, _ in picked}
    extra = [
        (s, vol_seen.get(s, 0.0))
        for s in sorted(tracked_symbols())
        if s not in have and s.endswith("USDT")
    ]
    if extra:
        picked.extend(extra)
        log(f"  → отбор: +{len(extra)} из журнала сверх лимита")

    stats["selected"] = len(picked)
    stats["from_journal"] = len(extra)

    return picked, stats
```

---

## 4. Объявление словаря

### было

```python
    tickers = get_futures_tickers()
    stats = {
```

### стало

```python
    tickers = get_futures_tickers()
    # Обороты всех USDT-пар, включая отсеянные. Нужен для добавки
    # журнала ниже: там встречаются монеты, не дошедшие до picked.
    vol_seen: dict[str, float] = {}
    stats = {
```

---

## Чего в этом патче нет

Вторая часть исходного патча — переписывание брифинга в
`render/brief.py`: удаление двух застывших фраз и статус журнала
вместо них. Не делаю в этом заходе намеренно: она удаляет строки
329–339 и перестраивает сборку массива `lines`, а `brief.py` я ещё не
читал целиком. После того как в первой части нашлась ошибка с
распаковкой пар, переносить остальное не глядя нельзя.
