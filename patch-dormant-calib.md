# Патч · порог отскока после первого замера

Один блок в `detectors/flow_config.py`. Сам подкейс `flow_dormant.py`
идёт целиком отдельно — в нём починен замер тишины.

Первый прогон со спячкой (14 августа, 148 монет) дал ноль срабатываний
и разложил отказы по гейтам:

- 84 — цикла не было: рост до пика x1.11…x2.84, медиана 1.54
- 26 — база не была тихой: 0.98…37.63, медиана 6.03
- 15 — отскока не было: 5.7…30%, медиана 15
- 10 — база широка
- 4 — падение мало

Двадцать шесть отказов по тишине — не рынок, а ошибка замера: окно
падения бралось фиксированным отступом и у монет с короткой историей
попадало в середину той же базы. Починено в подкейсе.

Порог отскока правится здесь. ACU дал 24.1%, APR ровно 30.0 и не
прошёл по строгому сравнению. Наблюдаемые «+50-150%» — это ход по
цене на глаз; замер по дневным барам в окне шестидесяти даёт меньше,
потому что берёт максимум и минимум внутри окна, а не размах
движения целиком.

Опускаю до 15: это медиана отвергнутых, то есть порог встаёт в
середину наблюдаемого разброса, а не выше его края.

---

## `detectors/flow_config.py`

### было

```python
DORMANT_BOUNCE_MIN = 30.0
```

### стало

```python
# Опущено с 30 после первого замера: ACU дал 24.1%, APR ровно 30.0.
# Прежнее значение стояло по ходу цены на глаз, новое — по медиане
# наблюдённого разброса отвергнутых (5.7…30, медиана 15).
DORMANT_BOUNCE_MIN = 15.0
```
