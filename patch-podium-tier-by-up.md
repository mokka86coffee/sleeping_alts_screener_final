# Ярус по росту от дна — тем же числом, что на карточке

## файл: `render/podium.py`

Применять ПОСЛЕ `patch-podium-tier-meter.md`. Отменяет
`patch-podium-recovered-fallback.md` — его применять не нужно.

Доля пройденного пути к пику жизни отвечала на вопрос «сколько ещё
осталось», а ярус спрашивает другое: ушла монета от базы или стоит на
ней. AVAAI +207% от дна оказывалась в «у дна», и экран противоречил
сам себе — на панели написано одно, стоит она в другом.

Порог теперь на той величине, которая на карточке и напечатана: рост
от минимума окна. Выше 200% — монета движется.

Побочный эффект важнее косметики. `upX` и `lifeDrop` приходят только
у монет со срабатыванием FLOW в текущем прогоне, а в зале таких
единицы: у 26 звёзд из 27 величин не было вовсе, доля выходила нулём,
и нижний ярус собирал всё подряд. `up` считается метриками для каждой
монеты выборки, а журнальные монеты возвращает в неё
`tracked_symbols` — значит величина есть всегда, и запасные пути не
нужны.

### было

```python
  /* Граница ярусов — доля пройденного пути от дна к пику ЖИЗНИ.
     Проценты, не кратность.

     Кратность от дна на эту роль не годится: она не знает, из какой
     ямы монета выбирается. HEMI на ×2.16 прошла 2.5% своего пути,
     BULLA на ×4.59 — 2.9%, а BTW на ×1.21 уже 76%, потому что
     падала всего на 21%. По кратности первые две «движутся», а
     третья стоит «у дна» — порядок обратный правильному.

     Граница по разбросу пробы 16 августа (61 монета): мин 0.07,
     q25 1.04, медиана 3.50, q75 14.82, макс 79.6. Двадцать стоит
     выше q75, и за ней начинается видимо другая популяция. */
  var TIER_MOVE_PCT = 20.0;

  /* Восстановленный путь из двух полей звезды, без новых величин в
     ядре. Вывод: цена = дно × upX, пик = дно / (1 − lifeDrop),
     отсюда доля = (upX − 1)·(1 − lifeDrop) / lifeDrop. Дно
     сокращается, поэтому сама цена не нужна.

     Ноль означает «не мерили»: величины нет, а не «монета на дне».
     Отправляем в нижний ярус — там она хотя бы попадётся на глаза,
     тогда как в «движется» утверждала бы то, чего мы не знаем.
     Падение ровно на сто процентов делит на ноль и означает, что
     пик жизни неизвестен, — тот же случай. */
  function recoveredPct(s) {
    var ux = +s.upX || 0;
    var ld = Math.abs(+s.lifeDrop || 0) / 100;
    if (ux <= 1 || ld <= 0 || ld >= 1) return 0;
    return (ux - 1) * (1 - ld) / ld * 100;
  }

  function tierOf(s) {
    return recoveredPct(s) >= TIER_MOVE_PCT ? 1 : 0;
  }
```

### стало

```python
  /* Граница ярусов — рост от минимума окна, в процентах.
     Выше двухсот монета уже едет.

     Это ровно то число, которое напечатано на самой панели, и в
     этом смысл. Прежняя мерка считала долю пройденного пути к пику
     жизни: она отвечает на вопрос «сколько потенциала осталось», а
     ярус спрашивает «ушла от базы или стоит на ней». Разные оси, и
     на экране это выглядело противоречием — AVAAI с подписью
     +207% от дна стояла в ярусе «у дна».

     Второе, и более важное: величина есть у КАЖДОЙ звезды. upX и
     lifeDrop приходят только у монет со срабатыванием FLOW в этом
     прогоне, а в зале таких единицы — у 26 из 27 доля выходила
     нулём, и нижний ярус собирал всех. up считается метриками для
     всей выборки, а журнальные монеты возвращает в неё
     tracked_symbols. */
  var TIER_MOVE_PCT = 200.0;

  /* Ноль означает «не мерили», а не «монета на дне». Отправляем в
     нижний ярус: там она попадётся на глаза, тогда как в «движется»
     утверждала бы то, чего мы не знаем. */
  function tierOf(s) {
    return (+s.up || 0) >= TIER_MOVE_PCT ? 1 : 0;
  }
```

### было

```python
      if (ti === 0) {
        list.sort(function (a, b) {
          var d = (+b.heldRallies || 0) - (+a.heldRallies || 0);
          return d || recoveredPct(a) - recoveredPct(b);
        });
      } else {
        list.sort(function (a, b) {
          return recoveredPct(a) - recoveredPct(b);
        });
      }
```

### стало

```python
      if (ti === 0) {
        list.sort(function (a, b) {
          var d = (+b.heldRallies || 0) - (+a.heldRallies || 0);
          return d || (+a.up || 0) - (+b.up || 0);
        });
      } else {
        list.sort(function (a, b) { return (+a.up || 0) - (+b.up || 0); });
      }
```
